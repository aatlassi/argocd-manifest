apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
  namespace: aatlassi
spec:
  ingressClassName: nginx
  rules:
  - host: "3.96.184.187"
    http:
      paths:
        - pathType: Prefix
          path: "/test"
          backend:
            service:
              name: myapp
              port:
                number: 80